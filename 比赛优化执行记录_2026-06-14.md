# 比赛优化执行记录

日期：2026-06-14

## 最终推荐

- 提交包：`runs/final_candidates/logic_timereise_condition_prior_rowcoord_bestproxy_submission.zip`
- 模型：`runs/candidates/logic_timereise_condition_prior_rowcoord_bestproxy.onnx`
- teacher score：`0.923149077933`
- macro F1：`0.988280286952`
- faith：`0.800064827874`
- simplicity：`0.901614574003`
- package valid：true
- zip sha256：`5801584363dfa83ab5534a248a618b4051a107843b2c2c9ff055cbacbdd2becd`

比上一版 `final_micro_morph`：F1 不变，faith 提升 `+0.00008355`，simplicity 小降，综合分提升约 `+0.00002444`。

## 执行结果

### A. GroupSegment-TimeREISE

全局融合没有超过起点。把 top GroupSegment 候选做 class row-coordinate 后有收益：

- 最好：`logic_timereise_group_segment_logit_rowcoord_bestproxy`
- teacher score：`0.921893267502`

结论：有局部行价值，但弱于后续 motif / condition 路线。

### B. Multi-scale b5/b10/b20

b5、b20 全局候选都不强，但 row-coordinate 有明显收益：

- 最好：`logic_timereise_multiscale_logit_rowcoord_bestproxy`
- teacher score：`0.922996531009`
- 采纳 class 2/6/8 的 b5 行

结论：多尺度应作为“行候选池”，不适合全局平均。

### C. Motif / Shapelet Prior

这是主提升来源之一：

- `logic_timereise_motif_prior_rowcoord_bestproxy`
- teacher score：`0.923124249051`
- faith：`0.799981274176`

后续 `final_micro_morph` 只是重新生成同权重图，simplicity 报告略高：

- `logic_timereise_final_micro_morph_bestproxy`
- teacher score：`0.923124636680`

结论：motif 方向有效，尤其适合按类别替换行。

### D. Offline Learned Mask Teacher

没有上训练型 mask 网络。只做了 micro morph 代理实验，没有带来 faith 提升。

结论：短期不建议继续加复杂 ONNX；如果要做，应离线训练后蒸馏成静态 `class,channel,time` 权重。

### E. Condition-aware Robust Aggregation

新增脚本：`tools/run_logic_timereise_condition_prior_search.py`

做法：

- 伪工况：RMS、crest、dominant lag、torque/PGB energy 分桶。
- proxy：`periodic_impulse`、`hybrid`、`impulse`。
- 先全局融合，完整 devkit 评估；没有超过起点。
- 再取 top 条件候选做 row-coordinate，最终提升。

最终采纳来源：

- 来源候选：`periodic_impulse_pgblag_median_pred_raw_power_m0005`
- 替换类别行：class 1、2、6、7、8
- 最终 teacher score：`0.923149077933`

## 排查过的问题

- 新脚本起始 tag 与当前最佳模型里的 `timereise_start_*` initializer 重名，已改成 `condition_start`。
- 旧 robust 脚本按旧 proxy 排名，不直接用于最终选择；最终一律按 `0.6*F1 + 0.3*faith + 0.1*simplicity` 排序。
- 所有被采纳候选都跑了完整 devkit；没有用局部 proxy 直接替换最终模型。
